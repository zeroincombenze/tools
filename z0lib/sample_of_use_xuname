#!/bin/bash
. ~/z0librc
f=$(xuname "-f")
if [ "$f" == "RHEL" ]; then
    echo "Found RedHat family OS"
elif [ "$f" == "Debian" ]; then
    echo "Found Debian family OS"
else
    echo "No Linux, maybe Unix or Posix"
fi
