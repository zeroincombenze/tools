#! /bin/bash
# -*- coding: utf-8 -*-
# Regression tests on lisa
#
THIS=$(basename $0)
TDIR=$(readlink -f $(dirname $0))
for x in "$TDIR" "." ".." "~" "/etc"; do
  if [ -e $x/z0librc ]; then
    . $x/z0librc
    Z0LIBDIR=$x
    Z0LIBDIR=$(readlink -e $Z0LIBDIR)
  fi
done
TESTDIR=$(findpkg "" "$TDIR . .." "tests")
RUNDIR=$(readlink -e $TESTDIR/..)
TRAVISDIR=$(findpkg "" "$TDIR . .. $HOME/dev" "_travis")
Z0TLIBDIR=$(findpkg "z0testrc" "$TDIR . .. $HOME/dev")
. $Z0TLIBDIR
Z0TLIBDIR=$(dirname $Z0TLIBDIR)


__version__=0.2.36.6

Z0BUG_init
parseoptest -l$TESTDIR/test_lisa.log $@
sts=$?
if [ $sts -ne 127 ]; then
  exit $sts
fi
# temporary
cp -v $RUNDIR/*.lish $TESTDIR/
cp -v $RUNDIR/*.sample $TESTDIR/
# end temp
UT1_LIST=""
UT_LIST="__version_V_${__version__}$RUNDIR/lisa"
UT_LIST="$UT_LIST lisa_test_01 lisa_test_02 lisa_test_03 lisa_test_04 lisa_test_05 lisa_test_06 lisa_test_07 lisa_test_08"
Z0BUG_main_file "" "$UT_LIST"
exit $?
